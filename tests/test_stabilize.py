"""Tests für die Stabilisierung — per TDD zu füllen.

Idee: ein synthetisches Bild um bekannte dx/dy (und ggf. Rotation) verschieben, durch
stabilize_series schicken und prüfen, dass die Rück-Transform es zurückholt (Referenz-
Modus) bzw. dass ein feature-armes Frame als Fehlschlag im Report auftaucht.
"""
import pytest

pytest.skip("stabilize noch nicht implementiert (TDD)", allow_module_level=True)
